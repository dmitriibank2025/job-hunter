# This file is intentionally a placeholder.
# At container start, CMD in the Dockerfile runs envsubst on
# default.conf.template and writes the result to /etc/nginx/conf.d/default.conf.
# This placeholder is overwritten at runtime — do not rely on it at build time.
#
# If you see CSP errors with connect-src blocking the API, the runtime
# substitution has failed: check that JOB_HUNTER_API_BASE_URL is set in the
# container environment.
#
# The authoritative nginx config is: frontend/nginx/default.conf.template
